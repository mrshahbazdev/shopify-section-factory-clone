<style data-shopify="">@font-face {
  font-family: "Bebas Neue";
  font-weight: 400;
  font-style: normal;
  font-display: swap;
  src: url("//section.store/cdn/fonts/bebas_neue/bebasneue_n4.135cdbcf397f167ca17d9c75224e0af26b4b4e55.woff2") format("woff2"),
       url("//section.store/cdn/fonts/bebas_neue/bebasneue_n4.3288a6929ba3287812eb6a03e362ee0bf5be1e3d.woff") format("woff");
}


  .section-template--24594750767433__ss_floating_menu_kUGdbx {
    border: solid  0px;
    margin-left: rem;
    margin-right: rem;
    border-radius: 5px;
  }

  .section-template--24594750767433__ss_floating_menu_kUGdbx-settings {
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    
      position: relative;
    
  }

  .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx {
    display: flex;
    flex-direction: column;
    justify-content: center;

    top: 15px;
    left: 15px;
    border-radius: 100%;
    
      background-color: #00a86e;
    
    gap: 4px;
    border: none;
    cursor: pointer;
    padding: 12px;
    aspect-ratio: 1/1;
    z-index: 9999;
    position: relative;
  }

  .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx span {
    width: 24px;
    height: 3px;
    background-color: #202020;
    transition: all 0.3s ease;
    border-radius: 2px;
    z-index: 3;
  }

  .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx.active span:nth-child(1) {
    transform: rotate(45deg) translate(4px, 4px);;
  }

  .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx.active span:nth-child(2) {
    opacity: 0;
  }

  .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx.active span:nth-child(3) {
    transform: rotate(-45deg) translate(6px, -6px);
  }

  .navigation-tabs-modal-template--24594750767433__ss_floating_menu_kUGdbx {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    
    background-color: #00a86e;
    
    z-index: 9998;
    display: none;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .navigation-tabs-modal-template--24594750767433__ss_floating_menu_kUGdbx.active {
    display: flex;
    opacity: 1;
  }

  
    .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx {
      background: transparent !important;
      border: 0px solid  !important;
      position: relative;
    }

    .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx .glass-filter-template--24594750767433__ss_floating_menu_kUGdbx,
    .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx .glass-overlay-template--24594750767433__ss_floating_menu_kUGdbx,
    .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx .glass-specular-template--24594750767433__ss_floating_menu_kUGdbx  {
      display: block;
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
    }

    .glass-filter-template--24594750767433__ss_floating_menu_kUGdbx {
      display: block;
      -webkit-backdrop-filter: blur(8px);
      backdrop-filter: blur(8px);
      -webkit-filter: url(#lensFilter-template--24594750767433__ss_floating_menu_kUGdbx);
      filter: url(#lensFilter-template--24594750767433__ss_floating_menu_kUGdbx);
      will-change: filter;
      -webkit-transform: translateZ(0);
      transform: translateZ(0);
      z-index: 0;
    }

    .glass-overlay-template--24594750767433__ss_floating_menu_kUGdbx {
      display: block;
      background: rgba(0, 0, 0, 0.15);
      -webkit-filter: saturate(1.2) brightness(1.1);
      filter: saturate(1.2) brightness(1.1);
      z-index: 1;
      transition: background .25s ease;
    }

    .glass-specular-template--24594750767433__ss_floating_menu_kUGdbx {
      display: block;
      box-shadow: inset 1px 1px 0 rgba(255,255,255,.4),
                  inset 0 0 6px rgba(255,255,255,.2);
      z-index: 2;
      transition: box-shadow .25s ease;
    }

    @media(min-width: 1024px) {
    .section-template--24594750767433__ss_floating_menu_kUGdbx {
      background: transparent !important;
      border: 0px solid  !important;
      position: relative;
    }

    .section-template--24594750767433__ss_floating_menu_kUGdbx .glass-filter-template--24594750767433__ss_floating_menu_kUGdbx,
    .section-template--24594750767433__ss_floating_menu_kUGdbx .glass-overlay-template--24594750767433__ss_floating_menu_kUGdbx,
    .section-template--24594750767433__ss_floating_menu_kUGdbx .glass-specular-template--24594750767433__ss_floating_menu_kUGdbx {
      display: block;
      position: absolute;
      inset: 0;
      border-radius: inherit;
      pointer-events: none;
    }

    .section-template--24594750767433__ss_floating_menu_kUGdbx-settings {
      position: relative;
      z-index: 3;
    }

    }
    
      .section-template--24594750767433__ss_floating_menu_kUGdbx,
      .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx {
        transition: transform .35s cubic-bezier(0.175, 0.885, 0.32, 2.2);
        will-change: transform;
      }

      .section-template--24594750767433__ss_floating_menu_kUGdbx:hover,
      .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx:hover { transform: scale(1.02); }

      .section-template--24594750767433__ss_floating_menu_kUGdbx:hover .glass-overlay-template--24594750767433__ss_floating_menu_kUGdbx,
      .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx:hover .glass-overlay-template--24594750767433__ss_floating_menu_kUGdbx { background: rgba(0,0,0,.25); }

      .section-template--24594750767433__ss_floating_menu_kUGdbx:hover .glass-specular-template--24594750767433__ss_floating_menu_kUGdbx,
      .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx:hover .glass-specular-template--24594750767433__ss_floating_menu_kUGdbx {
        box-shadow: inset 1px 1px 0 rgba(255,255,255,.55),
                    inset 0 0 8px rgba(255,255,255,.3);
      }
    
  

  .navigation-tabs-body-template--24594750767433__ss_floating_menu_kUGdbx {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    gap: 16px;
    padding-left: 1.5rem;
    padding-right: 1.5rem;
    margin-left: -1.5rem;
    margin-right: -1.5rem;
    position: relative;
    justify-content: center;
    height: 100%;
  }

  .navigation-tabs-body-template--24594750767433__ss_floating_menu_kUGdbx::-webkit-scrollbar {
    display: none;
  }

  .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx {
    margin-top: 0px;
    width: 100%;
    max-width: fit-content;
    min-width: max-content;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 32px;
    font-weight: 700;
    color: #202020;
    line-height: 100%;
    text-align: center;
    text-transform: unset;
    gap: 10px;
    text-decoration: none;
    padding: 6px;
    border-radius: 6px;
    border: 0px;
    border: none;
    background-color: transparent;
    transition: all 0.3s ease 0s;
    box-shadow: none;
    cursor: pointer;
    position: relative;
    background-color: rgba(0,0,0,0);
    border: 0px solid #ff8552;
    
      background-color: transparent !important;
      border: 0 !important;
    
    margin-bottom: 1rem;
  }

  
    .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx {
      transition: color 0.3s ease;
    }
    .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx:hover {
      background-color: transparent !important;
      border-color: transparent !important;
    }
    .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx.active {
      background-color: transparent !important;
      border-color: transparent !important;
    }

    .slide-indicator-template--24594750767433__ss_floating_menu_kUGdbx {
      display: none !important;
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      background-color: #00a86e;
      border-radius: 6px;
      transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      z-index: 0;
    }
  

  .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx:hover {
    color: #ffffff;
    background-color: #297373;
    border: 0px solid #ff8552;
    transition: all 0.3s ease 0s;
  }

  .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx.active {
    color: #ffffff;
    background-color: #00a86e;
    border: 0px solid #ff8552;
    transition: all 0.3s ease 0s;
  }

  .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx svg {
    opacity: 0;
    width: 29px;
    height: 29px;
    transition: all 0.3s ease 0s;
  }

  .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx:hover svg {
    opacity: 1;
    transition: all 0.3s ease 0s;
  }

  .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx.active svg {
    opacity: 1;
    transition: all 0.3s ease 0s;
  }

  .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx:hover svg path {
    transition: all 0.3s ease 0s;
    fill: #ffffff;
  }

  .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx.active svg path {
    transition: all 0.3s ease 0s;
    fill: #ffffff;
  }

  @media(min-width: 1024px) {



    .navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx {
      display: none;
    }

    .navigation-tabs-modal-template--24594750767433__ss_floating_menu_kUGdbx {
      position: relative;
      width: auto;
      height: auto;
      background: none;
      display: block !important;
      opacity: 1 !important;
      z-index: auto;
    }

    .section-template--24594750767433__ss_floating_menu_kUGdbx {
      margin-left: rem;
      margin-right: rem;
      border-radius: 8px;
      
      
    }

    .section-template--24594750767433__ss_floating_menu_kUGdbx-settings {
      padding: 0 5rem;
      padding-top: 8px;
      padding-bottom: 8px;
      padding-left: 8px;
      padding-right: 8px;
    }

    .navigation-tabs-body-template--24594750767433__ss_floating_menu_kUGdbx {
      gap: 8px;
      padding-left: 0rem;
      padding-right: 0rem;
      margin-left: 0rem;
      margin-right: 0rem;
      flex-direction: row;
      height: auto;
    }

    .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx {
      padding: 0 12px;
      padding-top: 12px;
      padding-bottom: calc(12px + 0px);
      font-size: 20px;
      
        padding-bottom: 12px;
      
      margin-bottom: 0rem;
    }

    .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx svg {
      width: 17px;
      height: 17px;
    }

    .slide-indicator-template--24594750767433__ss_floating_menu_kUGdbx {
      display: block !important;
    }
  }</style>
<style>
    .section-template--24594750767433__ss_floating_menu_kUGdbx-settings {
      max-width: clamp(100px, var(--page-content-width, var(--page-width)), 100%);
    }
  </style>
<style>
    .navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx {
      font-family: "Bebas Neue", sans-serif;
      font-weight: 400;
      font-style: normal;
    }
  </style>
<style>
    @media(min-width: 1024px) {
      .navigation-tabs-body-template--24594750767433__ss_floating_menu_kUGdbx {
        justify-content: start;
      }
    }
  </style>
<svg style="display: none;" xmlns="http://www.w3.org/2000/svg">
<filter filterunits="objectBoundingBox" height="100%" id="lensFilter-template--24594750767433__ss_floating_menu_kUGdbx" width="100%" x="0%" y="0%">
<fecomponenttransfer in="SourceAlpha" result="alpha">
<fefunca type="identity"></fefunca>
</fecomponenttransfer>
<fegaussianblur in="alpha" result="blur" stddeviation="50"></fegaussianblur>
<fedisplacementmap in="SourceGraphic" in2="blur" scale="50" xchannelselector="A" ychannelselector="A"></fedisplacementmap>
</filter>
</svg>
<div class="section-template--24594750767433__ss_floating_menu_kUGdbx navigation-tabs-template--24594750767433__ss_floating_menu_kUGdbx">
<div class="glass-filter-template--24594750767433__ss_floating_menu_kUGdbx"></div>
<div class="glass-overlay-template--24594750767433__ss_floating_menu_kUGdbx"></div>
<div class="glass-specular-template--24594750767433__ss_floating_menu_kUGdbx"></div>
<div class="section-template--24594750767433__ss_floating_menu_kUGdbx-settings">
<button aria-controls="modal-template--24594750767433__ss_floating_menu_kUGdbx" aria-expanded="false" class="navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx">
<span></span>
<span></span>
<span></span>
<div class="glass-filter-template--24594750767433__ss_floating_menu_kUGdbx"></div>
<div class="glass-overlay-template--24594750767433__ss_floating_menu_kUGdbx"></div>
<div class="glass-specular-template--24594750767433__ss_floating_menu_kUGdbx"></div>
</button>
<div class="navigation-tabs-modal-template--24594750767433__ss_floating_menu_kUGdbx" id="modal-template--24594750767433__ss_floating_menu_kUGdbx">
<div class="navigation-tabs-body-template--24594750767433__ss_floating_menu_kUGdbx">
<div class="slide-indicator-template--24594750767433__ss_floating_menu_kUGdbx" style="opacity: 0;"></div>
</div>
</div>
</div>
</div>
<script>
function initNavigationTabs() {
  const enableSlideEffect = true;
  let sections = document.querySelectorAll('.shopify-section'); 
  let body = document.querySelector('.navigation-tabs-body-template--24594750767433__ss_floating_menu_kUGdbx');
  const indicator = enableSlideEffect ? document.querySelector('.slide-indicator-template--24594750767433__ss_floating_menu_kUGdbx') : null;
  const toggle = document.querySelector('.navigation-tabs-toggle-template--24594750767433__ss_floating_menu_kUGdbx');
  const modal = document.querySelector('.navigation-tabs-modal-template--24594750767433__ss_floating_menu_kUGdbx');
  let titles = "Image banner,Multicolumn,About us";
  let titlesArray = titles.split(",");
  let belowThisSection = false;
  let count = 0;
  let isHovering = false;
  let hoverTimeout = null;
  
  if (toggle && modal) {
    toggle.addEventListener('click', () => {
      const isActive = toggle.classList.contains('active');
      
      if (isActive) {
        toggle.classList.remove('active');
        modal.classList.remove('active');
        toggle.setAttribute('aria-expanded', 'false');
        document.body.style.overflow = '';
      } else {
        toggle.classList.add('active');
        modal.classList.add('active');
        toggle.setAttribute('aria-expanded', 'true');
        document.body.style.overflow = 'hidden';
      }
    });
  }
  
  if (sections) { 
    sections.forEach((el, i) => {
      let itemId = el.id;
      if (![...el.children].some(child => child.tagName === 'TEMPLATE')) { 
    
        if(itemId != "shopify-section-header" && !itemId.includes("footer") && itemId != "shopify-section-announcement-bar") {
          if(belowThisSection) {
            let anchorText = titlesArray[count] || "Title";
            let anchor = `<a href="#${itemId}" class="navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx">
                ${anchorText}</a>`;
            body.insertAdjacentHTML('beforeend', anchor);  
            count++;
          }
          if(itemId == "shopify-section-template--24594750767433__ss_floating_menu_kUGdbx") {
            belowThisSection = true;
          }
        }
      } 
    }); 
  }

  const positionIndicator = (target) => {
    if (!enableSlideEffect || !indicator || !target) return;

    const bodyRect = body.getBoundingClientRect();
    const targetRect = target.getBoundingClientRect();

    if (targetRect.width === 0 || targetRect.height === 0) return;

    const leftPosition = targetRect.left - bodyRect.left + body.scrollLeft;
    const targetWidth = targetRect.width;

    requestAnimationFrame(() => {
      indicator.style.opacity = '1';
      indicator.style.width = `${targetWidth}px`;
      indicator.style.transform = `translateX(${leftPosition}px)`;
    });
  };

  const updateActiveState = (target) => {
    let items = document.querySelectorAll('.navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx');
    items.forEach(el => {
      el.classList.remove('active');
      el.style.transform = 'scale(1)';
    });
    target.classList.add('active');
    target.style.transform = 'scale(1)';

    if (!isHovering) {
      positionIndicator(target);
    }
  };

  const handleResize = () => {
    if (hoverTimeout) {
      clearTimeout(hoverTimeout);
    }
    
    hoverTimeout = setTimeout(() => {
      const activeItem = document.querySelector('.navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx.active');
      if (activeItem && !isHovering) {
        positionIndicator(activeItem);
      }
    }, 100);
  };

  setTimeout(() => {
    let items = document.querySelectorAll('.navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx');
  
    if (items && items.length > 0) { 
      updateActiveState(items[0]);

      items.forEach(el => {
    
        if (enableSlideEffect) {
          el.addEventListener('mouseenter', () => {
            isHovering = true;

            if (hoverTimeout) {
              clearTimeout(hoverTimeout);
              hoverTimeout = null;
            }
            
            positionIndicator(el);
          });
          
          el.addEventListener('mouseleave', () => {
            isHovering = false;
            
            if (hoverTimeout) {
              clearTimeout(hoverTimeout);
            }
            
            hoverTimeout = setTimeout(() => {
              const activeItem = document.querySelector('.navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx.active');
              if (activeItem) {
                positionIndicator(activeItem);
              }
            }, 150);
          });
        }
    
        el.addEventListener('click', (event) => { 
          event.preventDefault();
          
          isHovering = false;
          
          updateActiveState(el);
          
          if (window.innerWidth < 1024) {
            toggle.classList.remove('active');
            modal.classList.remove('active');
            toggle.setAttribute('aria-expanded', 'false');
            document.body.style.overflow = '';
          }
          
          const targetId = el.getAttribute('href'); 
          if (targetId) { 
            const targetElement = document.querySelector(targetId); 
            if (targetElement) { 
              const elementPosition = targetElement.getBoundingClientRect().top + window.scrollY;
              const offsetPosition = elementPosition - 100;
        
              window.scrollTo({ 
                top: offsetPosition, 
                behavior: 'smooth'
              }); 
            } 
          } 
        });
      });
    }

    if(sections) {
      sections.forEach(section => {
        if(section.id.includes('template--24594750767433__ss_floating_menu_kUGdbx')) {
          if (window.innerWidth < 1024) {
            section.style.position = 'fixed';
            section.style.left = '0';
            section.style.top = '0';
            section.style.zIndex = '5';
          } else {
            section.style.position = 'fixed';
            section.style.left = '50%';
            section.style.transform = 'translateX(-50%)';
            section.style.bottom = '60px';
            section.style.zIndex = '5';
            section.style.borderRadius = '8px';
            
          }
        }
      })
    }

    if (enableSlideEffect) {
      // Используем debounced resize handler
      window.addEventListener('resize', handleResize);
      
      // Инициализируем позицию индикатора
      const initialActive = document.querySelector('.navigation-tabs-item-template--24594750767433__ss_floating_menu_kUGdbx.active');
      if(initialActive) {
        // Небольшая задержка для корректного расчёта размеров
        requestAnimationFrame(() => {
          positionIndicator(initialActive);
        });
      }
    }

  }, 400);  
}

document.addEventListener('DOMContentLoaded', initNavigationTabs);
</script>
{% schema %}
{
  "name": "Floating menu",
  "tag": "section",
  "presets": [
    {
      "name": "Floating menu"
    }
  ]
}
{% endschema %}
