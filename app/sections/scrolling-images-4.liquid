<style data-shopify="">.section-template--22396074885449__ss_scrolling_images_4_hPCAFd {
    border-top: solid #000000 0px;
    border-bottom: solid #000000 0px;
    margin-top: 0px;
    margin-bottom: 0px;
    margin-left: 0rem;
    margin-right: 0rem;
    border-radius: 0px;
    overflow: hidden;
    position: relative;
  }
  
  .section-template--22396074885449__ss_scrolling_images_4_hPCAFd-settings {
    margin: 0 auto;
    padding-top: 27px;
    padding-bottom: 27px;
    padding-left: 0rem;
    padding-right: 0rem;
  }

 .scrolling-scroll-body-template--22396074885449__ss_scrolling_images_4_hPCAFd {
    overflow: hidden;
    white-space: nowrap;
    scrollbar-width: none;
    width: 100%;
    margin-left: 0;
  }

  .scrolling-scroll-body-template--22396074885449__ss_scrolling_images_4_hPCAFd::-webkit-scrollbar {
    display: none;
  }

  .scrolling-body-template--22396074885449__ss_scrolling_images_4_hPCAFd {
    display: flex;
    gap: 8px;
    padding-left: 8px;
    transform: translateX(0);
    transition: transform 0.1s ease-out;
    position: relative;
    left: 0;
  }

  .scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd {
    overflow: hidden;
    border-radius: 24px;
    border: 0px solid #000000;
    width: 180px;
    flex-shrink: 0;
  }

  .scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd img,
  .scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd svg {
    display: inline-block;
    width: 100%;
    height: 100%;
    object-fit: cover
  }

  .scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd svg {
    background-color: #AFAFAF;
  }
  
  @media(min-width: 1024px) {
    .section-template--22396074885449__ss_scrolling_images_4_hPCAFd {
      margin-top: 0px;
      margin-bottom: 0px;
      margin-left: 0rem;
      margin-right: 0rem;
      border-radius: 0px;
    }
    
    .section-template--22396074885449__ss_scrolling_images_4_hPCAFd-settings {
      padding: 0 5rem;
      padding-top: 36px;
      padding-bottom: 36px;
      padding-left: 0rem;
      padding-right: 0rem;
    }

    .scrolling-body-template--22396074885449__ss_scrolling_images_4_hPCAFd {
      gap: 16px;
    }

    .scrolling-scroll-body-template--22396074885449__ss_scrolling_images_4_hPCAFd {
      padding-left: 16px; /* Desktop padding equal to desktop gap */
    }

    .scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd {
      width: 300px;
    }
  }</style>
<style>
    .scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd {
      aspect-ratio: 9.6/13;
    }
  </style>
<style>
    @media(min-width: 1024px) {
      .scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd {
        aspect-ratio: 9.6/13;
      }
    }
  </style>
<div class="section-template--22396074885449__ss_scrolling_images_4_hPCAFd scrolling-template--22396074885449__ss_scrolling_images_4_hPCAFd" data-scroll-speed="0.7" style="background-color:#ffffff; background-image: ;">
<div class="section-template--22396074885449__ss_scrolling_images_4_hPCAFd-settings">
<div class="scrolling-scroll-body-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<div class="scrolling-body-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<div class="scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<img alt="" loading="lazy" src="//section.store/cdn/shop/files/jeffery-erhunse-vp9mRauo68c-unsplash.jpg?v=1727977303"/>
</div>
<div class="scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<img alt="" loading="lazy" src="//section.store/cdn/shop/files/pexels-godisable-jacob-226636-2703202.jpg?v=1728062070"/>
</div>
<div class="scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<img alt="" loading="lazy" src="//section.store/cdn/shop/files/pexels-jacob-sierra-419902407-15782944.jpg?v=1719852712"/>
</div>
<div class="scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<img alt="" loading="lazy" src="//section.store/cdn/shop/files/pexels-ivan-samkov-8498756.jpg?v=1728066164"/>
</div>
<div class="scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<img alt="" loading="lazy" src="//section.store/cdn/shop/files/prince-akachi-LWkFHEGpleE-unsplash.jpg?v=1717073204"/>
</div>
<div class="scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<img alt="" loading="lazy" src="//section.store/cdn/shop/files/pexels-marvin-malmis-ponce-272683079-12994664.jpg?v=1728068365"/>
</div>
<div class="scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<img alt="" loading="lazy" src="//section.store/cdn/shop/files/pexels-mikhail-nilov-7624800.jpg?v=1717075806"/>
</div>
<div class="scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<img alt="" loading="lazy" src="//section.store/cdn/shop/files/pexels-cottonbro-9836516.jpg?v=1729125683"/>
</div>
<div class="scrolling-image-template--22396074885449__ss_scrolling_images_4_hPCAFd">
<img alt="" loading="lazy" src="//section.store/cdn/shop/files/pexels-blue-bird-7243311.jpg?v=1729125166"/>
</div>
</div>
</div>
</div>
</div>
<script>
  class ScrollingImages {
    constructor(sectionId) {
      this.sectionId = sectionId;
      this.scrollBody = document.querySelector(`.scrolling-body-${sectionId}`);
      this.section = document.querySelector(`.section-${sectionId}`);
      this.baseSpeedMultiplier = parseFloat(this.section.getAttribute('data-scroll-speed') || 0.3);
      this.isMobile = window.innerWidth <= 768;
      // Halve the speed on mobile
      this.speedMultiplier = this.isMobile ? this.baseSpeedMultiplier * 0.3 : this.baseSpeedMultiplier;
      this.ticking = false;
      this.initialized = false;
      
      setTimeout(() => this.init(), 50);
    }
    
    init() {
      if (!this.scrollBody || !this.section || this.initialized) return;
      
      // Reset transform
      this.scrollBody.style.transform = 'translateX(0)';
      
      // Calculate initial dimensions
      this.recalculateDimensions();
      
      // Bind methods
      this.onScroll = this.onScroll.bind(this);
      this.updateScrollPosition = this.updateScrollPosition.bind(this);
      this.handleResize = this.handleResize.bind(this);
      
      // Add event listeners
      window.addEventListener('scroll', this.onScroll, { passive: true });
      window.addEventListener('resize', this.handleResize);
      
      // Mark as initialized
      this.initialized = true;
      
      // Initial update
      this.updateScrollPosition();
    }
    
    recalculateDimensions() {
      const rect = this.section.getBoundingClientRect();
      this.sectionWidth = rect.width;
      this.totalWidth = this.scrollBody.scrollWidth;
      this.maxScroll = Math.max(0, this.totalWidth - this.sectionWidth);
    }
    
    updateScrollPosition() {
      const rect = this.section.getBoundingClientRect();
      const viewportHeight = window.innerHeight;
      
      let progress = 0;
      
      // Only start if section is approaching view and continue until completely out of view
      if (rect.bottom >= 0) { // Changed condition to continue until section is gone
        const totalDistance = rect.height + viewportHeight; // Add viewport height to total distance
        const startTrigger = viewportHeight - 500; // Start 500px before section reaches top
        
        if (rect.top <= startTrigger) {
          const traveled = startTrigger - rect.top;
          progress = Math.max(0, Math.min(1, traveled / totalDistance));
        }
      } else {
        // Section has scrolled completely out of view
        progress = 1;
      }
      
      // Use the speed multiplier here
      const scrollOffset = Math.max(0, progress * this.maxScroll * this.speedMultiplier);
      
      requestAnimationFrame(() => {
        this.scrollBody.style.transform = `translateX(-${scrollOffset}px)`;
      });
    }
    
    onScroll() {
      if (!this.ticking) {
        requestAnimationFrame(() => {
          this.updateScrollPosition();
          this.ticking = false;
        });
        this.ticking = true;
      }
    }
    
    handleResize() {
      clearTimeout(this.resizeTimeout);
        this.resizeTimeout = setTimeout(() => {
          this.isMobile = window.innerWidth <= 768;
          // Update speed multiplier when screen size changes
          this.speedMultiplier = this.isMobile ? this.baseSpeedMultiplier * 0.3 : this.baseSpeedMultiplier;
          this.recalculateDimensions();
          this.scrollBody.style.transform = 'translateX(0)';
          this.updateScrollPosition();
        }, 100);
    }
    
    destroy() {
      if (this.initialized) {
        window.removeEventListener('scroll', this.onScroll);
        window.removeEventListener('resize', this.handleResize);
        clearTimeout(this.resizeTimeout);
      }
    }
  }

  // Store all section instances
  window.scrollingSections = {};

  // Initialize all sections
  function initAllScrollingSections() {
    // Clean up existing instances
    Object.values(window.scrollingSections).forEach(instance => instance.destroy());
    window.scrollingSections = {};
    
    // Initialize new instances
    document.querySelectorAll('[class*="section-"]').forEach(section => {
      const sectionId = section.className.match(/section-([^\s]+)/)?.[1];
      if (sectionId && section.querySelector(`.scrolling-body-${sectionId}`)) {
        window.scrollingSections[sectionId] = new ScrollingImages(sectionId);
      }
    });
  }

  // Initialize on load and after DOM content loaded
  initAllScrollingSections();
  document.addEventListener('DOMContentLoaded', initAllScrollingSections);

  // Handle Shopify theme editor
  if (Shopify.designMode) {
    document.addEventListener('shopify:section:unload', (event) => {
      const sectionId = event.detail.sectionId;
      if (window.scrollingSections[sectionId]) {
        window.scrollingSections[sectionId].destroy();
        delete window.scrollingSections[sectionId];
      }
    });
    
    document.addEventListener('shopify:section:load', () => {
      setTimeout(initAllScrollingSections, 50);
    });
    
    document.addEventListener('shopify:section:reorder', () => {
      setTimeout(initAllScrollingSections, 50);
    });
  }
</script>
{% schema %}
{
  "name": "Scrolling images #4",
  "tag": "section",
  "presets": [
    {
      "name": "Scrolling images #4"
    }
  ]
}
{% endschema %}
