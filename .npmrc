# Install production dependencies only on shared hosting to avoid build tools
# like esbuild that fail in restricted cPanel/CloudLinux environments.
# To install dev dependencies for local development, run:
# npm install --include=dev
omit=dev
